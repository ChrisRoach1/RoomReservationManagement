@model IEnumerable<RoomReservationManagement.Models.res_reviews>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="http://ajax.googleapis.com/ajax/libs/jquery/2.0.0/jquery.min.js"></script>
<script src="../../Content/star-rating-svg-master/dist/jquery.star-rating-svg.js"></script>
<link rel="stylesheet" type="text/css" href="../../Content/star-rating-svg-master/src/css/star-rating-svg.css">






<h1>Room Reviews</h1>
<hr />
<style>
    .room {
        padding: 15px;
        background-color: #fafafa;
        border-left: 6px solid #7f7f84;
        margin-bottom: 10px;
        -webkit-box-shadow: 0 5px 8px -6px rgba(0,0,0,.2);
        -moz-box-shadow: 0 5px 8px -6px rgba(0,0,0,.2);
        box-shadow: 0 5px 8px -6px rgba(0,0,0,.2);
        border-color: #80D651;
    }

    .button {
        background-color: #4CAF50; /* Green */
        border: none;
        color: white;
        padding: 7px 16px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
    }

    #panorama {
        width: 600px;
        height: 400px;
    }
</style>


<body>


	<div style="height:10px"> </div>

	@*<button class="button">
			@Html.ActionLink("Room 2 Review Details", "Details", "Review")
		</button>*@
	<button class="button">
		@Html.ActionLink("Create Review", "NewReview", "Review")
	</button>


	@foreach (var item in Model)
	{

		<div class="room">
			<h1 style="color:#80D651;">@Html.DisplayFor(modelItem => item.Res_Rooms.room_name)   <div class="my-rating-set"></div></h1>
			<p>
				@Html.DisplayFor(modelItem => item.review)
			</p>
			<p>
				@Html.DisplayFor(modelItem => item.audit_create_dt)
			</p>
		</div>


		<script>
			$(".my-rating").starRating({
				starSize: 25,
				hoverColor: '#4CAF50',
				ratedColor: 'orange',
				useGradient: false,
				disableAfterRate: false,
				starGradient: {
					start: '#FEF7CD',
					end: '#FF9511'
				},
				callback: function (currentRating, $el) {
					// make a server call here
				}
			});
		</script>
		<script>
        $(".my-rating-set").starRating({
            readOnly: true,
            initialRating: @Html.DisplayFor(modelItem => item.rating),
        starSize: 25,
            callback: function (currentRating, $el) {
                // make a server call here
            }
                    });
		</script>
	}
	<br />
</body>

